services:
  - type: web
    name: presquile-le-pouliguen
    runtime: static
    buildCommand: cd web && npm ci && npm run build
    staticPublishPath: web/dist
    headers:
      # Vite fingerprints everything under /assets, so the filename changes
      # whenever the bytes do. Render's default is max-age=0, which makes every
      # cold session re-download the whole bundle.
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /*
        name: Strict-Transport-Security
        value: max-age=31536000; includeSubDomains; preload
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
      - path: /*
        name: Permissions-Policy
        value: camera=(), microphone=(), geolocation=(self), payment=(), usb=()
      - path: /*
        name: Content-Security-Policy
        value: >-
          default-src 'self'; base-uri 'self'; object-src 'none';
          frame-ancestors 'none'; form-action 'none'; script-src 'self' 'unsafe-inline'
          https://www.googletagmanager.com https://www.google-analytics.com;
          style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
          font-src 'self' https://fonts.gstatic.com data:; img-src 'self'
          data: blob: https:; connect-src 'self' https://api.open-meteo.com
          https://marine-api.open-meteo.com https://proxy.transport.data.gouv.fr
          https://data.capatlantique.fr https://data.loire-atlantique.fr
          https://www.google-analytics.com https://region1.google-analytics.com
          https://analytics.google.com;
          frame-src 'self'; manifest-src 'self'; worker-src 'self' blob:
